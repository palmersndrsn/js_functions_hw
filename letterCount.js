var letterCount = function (word) {
	count = 0
	for ( var i = 0; i < word.length; i++) {
		count += (word[i] )
		{

		}		
	}
	return word[i] + i
}


console.log(letterCount("apple"))

//not done, can't figure out what to do.